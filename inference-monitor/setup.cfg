[metadata]
name = sentinel-inference-monitor
version = attr: sentinel_inference.__version__

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.11

[options.packages.find]
where = src

[tool:pytest]
asyncio_mode = auto
testpaths = tests
